"""
URL configuration for risk app
"""

from django.urls import path
from . import views

app_name = 'risk'

urlpatterns = [
    # Risk management URLs will be added here
    # Example: path('limits/', views.risk_limits, name='limits'),
]
