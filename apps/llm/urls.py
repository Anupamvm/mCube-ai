"""
URL configuration for LLM app
"""

from django.urls import path
from . import views

app_name = 'llm'

urlpatterns = [
    # LLM integration URLs will be added here
    # Example: path('validate/', views.validate_strategy, name='validate'),
]
