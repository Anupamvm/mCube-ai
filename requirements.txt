# Django
Django==4.2.7
django-environ==0.11.2

# Database (SQLite is default, no extra driver needed)

# Celery & Task Queue (used for all background tasks)
celery==5.3.4  # Distributed task queue
redis==5.0.1  # Redis client for Celery broker/backend

# API Clients
requests==2.31.0
websocket-client>=1.7.0  # Updated for neo-api-client compatibility

# Broker APIs
breeze-connect==1.0.62
# kotak-neo-api is included in the repo - install manually: pip install -e ./kotak-neo-api
yfinance>=0.2.31  # Yahoo Finance for market data

# Data Processing
pandas>=2.2.0
numpy>=1.26.0,<2.0  # Pin to 1.x for scipy compatibility (NumPy 2.0 breaks scipy)
openpyxl>=3.1.5  # Excel file support (required for Trendlyne XLSX files)
ta>=0.11.0  # Technical Analysis library
# ta-lib==0.4.28  # Commented out - requires manual installation

# LLM & AI
chromadb==0.4.18
sentence-transformers==2.2.2
openai>=1.0.0  # OpenAI client for vLLM and LLM services

# Web Scraping
beautifulsoup4>=4.12.2
html5lib>=1.1  # Parser for BeautifulSoup
selenium>=4.15.2
chromedriver-autoinstaller>=0.6.4

# PDF Processing
pdfplumber>=0.10.0  # PDF text extraction for analyst reports

# Alerts & Notifications
python-telegram-bot>=21.0  # Requires v21+ for Python 3.13 compatibility
telethon==1.33.1  # For advanced Telegram features
twilio==8.10.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3
pyotp>=2.9.0  # TOTP generation for Kotak Neo v2 auto-login

# Family Portfolio Intelligence
djangorestframework>=3.14.0   # REST API for investments app
django-cors-headers>=4.3.0    # CORS for Next.js frontend
casparser>=2.4.0              # CAS PDF parser (NSDL, CAMS, KFintech)
pyxirr>=0.9.0                 # XIRR calculation
reportlab>=4.1.0              # PDF report generation
cryptography>=41.0.0          # AES-256 encryption for CAS file storage

# Development
pytest==7.4.3
pytest-django==4.7.0
black==23.11.0
